<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.sajo.completed">
	
<!-- gNum의 최대값 -->
<select id="maxNum" resultType="int">
	select nvl(max(cpNum),0) from complete	
</select>

<!-- gNum의 최대값 -->
<select id="gnoMaxNum" resultType="int">
	select nvl(max(gNo),0) from complete	
</select>

<!-- 데이터 입력 -->
<insert id="insertData" parameterType="com.sajo.dto.CompletedDTO">
	insert into complete (gNo,cpNum,cHitCount,mId,gSubject,imgnum,
	gContent,gCreated,boardName,gRecomm) values (#{gNo},#{gNum},0,#{mId},#{gSubject},#{imgNum},
	#{gContent},sysdate,#{boardName},0)
</insert>

<!-- 전체데이터 갯수 -->
<select id="getDataCount" parameterType="hashMap" resultType="int">
	select nvl(count(*),0) from complete
	where ${searchKey} like '%' || #{searchValue} || '%'
</select>

<!-- 모든 데이터 -->
<select id="getLists" parameterType="hashMap" resultType="com.sajo.dto.CompletedDTO">
	select * from (
	select rownum rnum, data.* from(
	select cpNum,mId,cSubject,gHitCount,to_char(gCreated,'YYYY-MM-DD') gCreated
	from complete where boardName=#{boardName} and ${searchKey} like '%' || #{searchValue} || '%'
	order by gNum desc) data)
<![CDATA[
	where rnum>=#{start} and rnum<=#{end}
]]>
</select>

<!-- 조회수 수정 -->
<update id="updateHitCount" parameterType="int">
	update complete set gHitCount=gHitCount+1 where gNum=#{gNum}
</update>

<!-- 한개의 데이터 -->
<select id="getReadData" parameterType="int" resultType="com.sajo.dto.CompletedDTO">
	select gNo,gNum,gHitCount,mId,gSubject,imgNum,
	gContent,gCreated,boardName from complete where gNum=#{gNum}
</select>

<!-- 데이터 수정 -->
<update id="updateData" parameterType="com.sajo.dto.CompletedDTO">
	update complete set gSubject=#{gSubject},
	gContent=#{gContent} where gNum=#{gNum}
</update>

<!-- 데이터 삭제 -->
<delete id="deleteData" parameterType="int">
	delete completed where gNum=#{gNum}
</delete>

<delete id="c_deleteData" parameterType="int">
	delete boardcomment where cpNum=#{cpNum}
</delete>

<select id="getbest" parameterType="int" resultType="com.sajo.dto.CompletedDTO">
	select * from (
	select rownum rnum, data.* from(
	select gno,crecomm,chitcount,ccategory,cprice,csubject,ccontent,to_char(cCreated,'YYYY-MM-DD') gCreated, imgnum 
	from complete order by crecomm desc) data)
<![CDATA[
	where rnum>=1 and rnum<=3
]]>
</select>


	
</mapper>